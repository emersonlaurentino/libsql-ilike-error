{
  "scripts": {
    "db:seed": "bun run src/db/seed.ts",
    "db:push": "bun drizzle-kit push:sqlite",
    "db:studio": "bun drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "libsql": "^0.1.34"
  },
  "devDependencies": {
    "bun-types": "^1.0.7",
    "drizzle-kit": "^0.20.1"
  }
}
